import React from "react";

const TimerButton = () => {
    //the component returns a button 
    return(
        <div>
            <button>Start</button>
        </div>
    )
}

export default TimerButton;